<h1>id: <%= @task.id %> のタスク編集ページ</h1>

<%= form_with(model: @task) do |f| %>

  <%= f.label :status, 'ステータス変更' %>
  <%= f.text_field :status %>

  <%= f.label :content, '変更後' %>
  <%= f.text_field :content %>

  <%= f.submit '変更' %>
<% end %>

<%= link_to '一覧に戻る', tasks_path %>